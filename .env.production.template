# Production Environment Configuration Template
# Copy this file to .env.production and fill in the values

# ============================================
# Application Configuration
# ============================================
NODE_ENV=production
PORT=3000

# ============================================
# Database Configuration
# ============================================
# Database host (use 'postgres' when running in Docker, or IP/hostname for external DB)
DB_HOST=postgres
DB_PORT=5432
DB_USERNAME=research_agent
DB_PASSWORD=CHANGE_THIS_TO_SECURE_PASSWORD
DB_DATABASE=research_agent_db

# ============================================
# Ollama Configuration
# ============================================
# Ollama service URL
# - Use http://host.docker.internal:11434 if Ollama runs on host machine
# - Use http://ollama:11434 if Ollama runs in Docker network
# - Use external URL if Ollama is hosted remotely
OLLAMA_BASE_URL=http://host.docker.internal:11434

# Ollama model to use for LLM operations
OLLAMA_MODEL=qwen2.5

# ============================================
# Tavily API Configuration
# ============================================
# Required: Get your API key from https://tavily.com
TAVILY_API_KEY=tvly-YOUR_API_KEY_HERE

# ============================================
# Web Fetch Configuration
# ============================================
# Timeout for web fetch operations (milliseconds)
WEB_FETCH_TIMEOUT=10000

# Maximum size for fetched content (bytes)
WEB_FETCH_MAX_SIZE=1048576

# ============================================
# Logging Configuration
# ============================================
# Log level: error, warn, info, http, verbose, debug, silly
LOG_LEVEL=info

# Log directory (relative to app root)
LOG_DIR=./logs

# ============================================
# Security Notes
# ============================================
# 1. NEVER commit this file with actual secrets to version control
# 2. Use strong, unique passwords for production
# 3. Rotate API keys regularly
# 4. Use environment-specific configurations
# 5. Consider using secrets management tools (e.g., Docker secrets, Vault)
